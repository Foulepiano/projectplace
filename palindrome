def string_palindrome(word):
    word = word.replace(" ", "").lower()
    
    return word == word[::-1]

string_word = input("Enter a string: ")

if string_palindrome(string_word):
    print("The string is a palindrome.")
else:
    print("The string is not a palindrome.")
